import React from "react";
import { Typography } from "@mui/material";
import { Center } from "./components/basic";

function App() {
  return (
    <Center sx={{ marginTop: 64 }}>
      <Typography variant={"h1"}>Compass Web</Typography>
    </Center>
  );
}

export default App;
